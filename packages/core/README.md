# @dsh-memory/core

Harness-agnostic memory types, hashed/lexical similarity, recency/importance/affect scoring, bounded Hebbian association scoring, token-budget packing, deterministic compaction, and trace contracts. This package imports no DeepSeek Harness modules and performs no model calls.
