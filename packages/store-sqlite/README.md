# @dsh-memory/store-sqlite

SQLite implementation of `MemoryStore` built on Node.js `node:sqlite`. It stores raw memory content, bounded affect hints, retrieval traces, and decaying association edges locally.
