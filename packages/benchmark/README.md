# @dsh-memory/benchmark

JSONL benchmark runner for retrieval traces and the first four feature ablations. Its current string-evidence hit rate is a pipeline check, not a publication-quality evaluation.
